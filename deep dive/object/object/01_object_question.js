//Create an object named person with properties like name, age, and country. Print each property to the console.

const person = {
  name : "Suryanshu" ,
  age : 18,
  country : "Nepal" ,       
};

console.log(person.name);
console.log(person.age);
console.log(person.country);