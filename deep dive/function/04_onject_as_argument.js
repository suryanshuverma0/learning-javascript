const person ={};

function personDetails(person) {
         person.name = "Suryanshu Verma";
         person.email = "veenasa676@gmail.com";
         person.nationality = "Nepali";
}

personDetails(person);
console.log(person);